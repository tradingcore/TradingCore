# 🔧 Config

Arquivos de configuração do TradingCore.

## 📁 Arquivos

- `credentials.json` - Credenciais do Google Cloud (não versionado)
- `rodar.sh` - Script para executar o sistema

## 🚀 Como usar

Execute de qualquer lugar:

```bash
./config/rodar.sh
```

Ou da raiz:

```bash
bash config/rodar.sh
```

